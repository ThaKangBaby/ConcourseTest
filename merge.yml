---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
- name: Ready-branch

run:
  path: echo
  args: ["testscript running"]
  #path: ci/scripts/testscript
